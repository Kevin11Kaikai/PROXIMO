"""New layered architecture package for PROXIMO."""

# This package will gradually replace the legacy `src/` implementation.
# Modules inside should provide stable interfaces that can wrap or
# re-export existing functionality while we refactor the internal layout.

